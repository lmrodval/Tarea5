package iteso.mx.tarea05.interfaces

interface HomeView{

    fun showRefreshView(fragmentid : Int)

    fun showProgress(fragmentid : Int)

    fun showNoInfo (fragmentid : Int)

    fun showInfo (fragmentid : Int)

}